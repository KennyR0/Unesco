/**
 * Vitest-only replacement for Next.js' server-only package.
 * The integration runner aliases this file so server modules can be imported
 * without pretending that a client bundle is executing them.
 */
export {};
